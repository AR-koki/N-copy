<div class=container>
	<div>
		<h4>話数の追加</h4>
		<%= form_with model:@item, url:admins_items_path do |f| %>
		<%= f.text_field :title %><br>
		<%= f.text_area :gist %><br>
		<%= f.submit '追加' %>
		<% end %>
	</div>
</div>