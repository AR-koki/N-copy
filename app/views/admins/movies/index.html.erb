<div class="container">
	<div>
		<h3>追加</h3>
		<%= link_to "投稿", new_admins_movie_path %>
	</div>
	<div>映画一覧</div>
		<% @movies.each do |movie| %>
		<%= link_to movie.title, admins_movie_path(movie) %>
		<% if movie.is_active %>
			<p>有効</p>
		<% else %>
			<p>無効</p>
		<% end %>
		<% end %>
</div>