<div class="container">
	<div>
		<h3>新規映画</h3>
		<%= form_with model: @movie, url:admins_movies_path do |f| %>
		<%= f.text_field :title %><br>
		<%= f.text_area :introduction %><br>
		<%= f.submit "追加" %>
		<% end %>
	</div>
</div>